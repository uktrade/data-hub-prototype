/* globals expect: true, describe: true, it: true */
'use strict';

const CompanyApp = require('../../src/apps/company.app');

describe('Company App', () => {
  describe('back link', () => {
    it('should link back to the homepage if that is the previous page');
    it('should link back to the search page if that is the previous page');
  });
});
